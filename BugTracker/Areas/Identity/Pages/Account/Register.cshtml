@page
@model RegisterModel
@{
    ViewData["Title"] = "Register";
}
<div class="text-center vh-100 d-flex" style="background-color:#808080; align-items: center;">
    <div class="text-center rounded-5 shadow bg-light p-3 w-100" style="max-width:420px; margin: 0 auto;">

@*        <form asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <h1 class="h3 mb-3 fw-normal">Bug Tracker</h1>
            <h1 class="h3 mb-3 fw-normal">Create a new account</h1>

            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email" class="sr-only"></label>
                <input asp-for="Input.Email" class="form-control" placeholder="Email address" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Input.Password" class="sr-only"></label>
                <input asp-for="Input.Password" class="form-control" placeholder="Password" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Input.ConfirmPassword" class="sr-only"></label>
                <input asp-for="Input.ConfirmPassword" class="form-control" placeholder="Confirm Password" />
                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
            </div>
            <div class="form-group">
                <button type="submit" class="w-100 btn btn-lg btn-primary">Register</button>
            </div>
        </form>*@
         <h6>Currently Registering and password recover is disabled as this project is still in the development phase. Please use the demo user menu that can be found on the login page to test the bug tracker out.</h6>
        <hr class="my-4">
        <small class="text-muted">
            <p>Back to <a asp-page="./Login" asp-route-returnUrl="@Model.ReturnUrl">Sign In</a></p>
        </small>
    </div>

    @section Scripts {
    <partial name="_ValidationScriptsPartial" />
    }
</div>
